{% extends "layout.jinja2" %}
{% block title %} Sera Shioda Smith - Learning Journal - Entries {% endblock %}

    <!-- Body Layout -->  

    {% block body %} 
        <body>
        {% for entry in entries %}
          {#   <div class="container">
                <div class="col-md-6 abt-left">
                    <a href="{{ request.route_url('detail', id=entry.id) }}""></a><img src="static/images/grumpydog2.jpeg" alt="" /></a>
                    <h6>{{ entry.title }}</h6>
                    <h3><a href="{{ request.route_url('detail', id=entry.id) }}">{{ entry.title1 }}</a></h3>
                        <p>{{ entry.body| truncate(500) }}</p>
                    <h4>Date Created: {{ entry.create_date }}</h4>
                </div>
                <div class="about-btn">
                    <a href="{{ request.route_url('detail', id=entry.id) }}">Read More</a>
                    <a href="{{ request.route_url('edit', id=entry.id) }}">Edit Entry</a>
                </div> #}
            {# </div> #}


            <!--banner-starts-->
            <div class="banner1">
                <div class="container">
                    <div class="banner1-top">
                        <div class="banner1-text">
                            <h2>{{ entry.title }}</h2>
                            <h1>{{ entry.title1 }}</h1>
                            <p>{{ entry.body }}</p>
                            <div class="about-btn">
                                <a href="{{ request.route_url('edit', id=entry.id) }}">Edit Post</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!--banner-end-->
        {% endfor %}
        </body>

    {% endblock %}

    <!-- end Body Layout -->



